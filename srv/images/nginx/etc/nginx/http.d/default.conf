server {
    listen 8888 default_server;

    location /healthcheck {
        access_log  off;
        error_log   off;
        return 200 'OK';
    }

    location = /stub_status {
        stub_status;
    }
}
